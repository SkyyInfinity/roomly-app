import { ScrollView, Text } from 'react-native';
import Title from '../../components/layouts/Title';

const forgot = () => {
    
	return <>
        <Title title="Mot de passe oublié" back/>
        <ScrollView>
            <Text>forgot</Text>
        </ScrollView>
    </>;
};

export default forgot;
